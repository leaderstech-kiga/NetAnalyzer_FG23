# 스니퍼 패킷 해독 가이드 — 현장에서 hex 만 보고 상태 판정

> **작성**: 2026-09-04 (S6a 실수신 이후)
> **대상**: `[S6a]` 로 찍히는 NetAnalyzer_FG23 스니퍼 수신 로그
> **근거**: `../LTD_W10D/Software/LTD_W10D_V03/rf_proto.h` (v1.1, VER=0x02)
>   · 헤더 `:160~167` · MSG_TYPE `:114~152` · 페이로드 `:186~257`
>   · 상수 `:32,37,78~80,194~198,244~245,267`
> ⚠ 이 문서는 **감지기 소스가 단일 기준**이다. rf_proto.h 가 바뀌면 여기도 고친다.
>   문서끼리 베끼지 말 것 (`RF_STEP구현_SYNC.md` 는 MSG 값 오기 전력 있음).

---

## 0. 30초 판독법

```
[S6a] #12 ch=101 rssi=-18 crc=PASS len=9
[S6a]   0000: 08 02 01 03 01 10 00 04 02
                ↑  ↑  ↑  ↑  ↑  └──┘ ↑  ↑
              LEN VER GRP SRC MSG  SEQ HOP payload
```

**보는 순서**

| 순서 | 볼 것 | 판정 |
|---|---|---|
| 1 | `crc=` | **FAIL 이면 내용 믿지 말 것.** 도달은 했으나 깨짐 |
| 2 | `[4]` MSG | 무슨 사건인가 (§2 표) |
| 3 | `[3]` SRC | 누가 보냈나 |
| 4 | `[7]` HOP | **4 = 원본 / 3 이하 = 중계본** (§3) |
| 5 | payload | 상세 (§4) |

---

## 1. 바이트 위치는 고정이다

RAIL 은 **wire 그대로** 준다 → `buf[0]` 이 LEN 이다. 헤더는 항상 7B.

```
offset   0     1     2     3     4     5     6     7     8 ...
        LEN   VER   GRP   SRC   MSG  SEQlo SEQhi  HOP   payload
        └───┘ └─────────────── 헤더 7B ──────────────┘
         본문 길이(헤더+payload)
```

- **`len=` 은 LEN+1** 이다 (LEN 바이트 자신이 포함 안 됨). `LEN=0x08` → `len=9`
- **SEQ 는 little-endian 2B.** `10 00` = 0x0010 = **16** (0x1000 아님)
- 나머지는 전부 1B 단순값

| 필드 | 정상값 | 이상하면 |
|---|---|---|
| `[1]` VER | **0x02** 고정 (v1.1) | 0x01 = 구버전 / 그 외 = 오수신·CRC 우연통과 |
| `[2]` GRP | 1~24 | 0 이나 25↑ = 잘못된 프레임 |
| `[3]` SRC | 1~61 일반 / 240,241,242 지원장비 | 0 = Setting mode 인데 송신됨(버그) |
| `[6]` HOP | 4=원본, 1=PING/PONG | §3 참조 |

**SRC 특수 주소** — `240`=수신기(RS-485 게이트웨이) `241`=외부전송(TCP/CDMA) `242`=점검장비(TOOL)

---

## 2. MSG_TYPE 전체 표 `buf[4]`

영역이 값으로 갈린다: **0x00~0x0F 알람(중계됨) / 0x10~0x1F 상태 / 0x20~0x2F 점검 / 0x30~0x3F 명령**

| MSG | 이름 | payload | len | 채널 | 무슨 뜻인가 |
|---|---|---|---|---|---|
| `0x01` | **ALARM_FIRE** | cause 1B | 9 | **101** 롱PA | ★화재. cause 로 원인 구분 |
| `0x02` | **ALARM_STOP** | 없음 | 8 | **101** 롱PA | ★경보 해제 |
| `0x03` | ALARM_PRE | cause 1B | 9 | 101 | 예비경보 (보류, 미사용) |
| `0x10` | HEARTBEAT | bat+flags 3B | 11 | 1 짧은PA | 생존 신고 |
| `0x11` | **STATUS_REPORT** | 16B | **24** | 1 짧은PA | ★상세 텔레메트리 (§4.2) |
| `0x12` | BATTERY_LOW | bat_mv 2B | 10 | 1 | 배터리 부족 |
| `0x13` | SENSOR_ERROR | err 1B | 9 | 1 | 센서 고장 |
| `0x14` | TAMPER | 없음 | 8 | 1 | 탈거 감지 |
| `0x15` | LINK_QUALITY | rssi,lqi,margin 3B | 11 | 1 | 링크 품질 |
| `0x16` | NEIGHBOR_REPORT | cnt+[id,rssi]×n | 가변 | 1 | 이웃 목록 |
| `0x18` | HELLO | 없음 | 8 | 1 | 24h 생존 (KFI W4) |
| `0x20` | TEST_REQ | 없음 | 8 | 1 | 점검 요청 |
| `0x21` | TEST_ACK | bat+flags 3B | 11 | 1 | 점검 응답 |
| `0x22` | SURVEY_REQ | 없음 | 8 | 1 | 현장조사 요청 |
| `0x23` | SURVEY_REPLY | 4B | 12 | 1 | rssi,lqi,margin,ngb_cnt |
| `0x24` | **LINK_PING** | **없음** | **8** | **101** 롱PA | ★부팅 자가진단. HOP=1 |
| `0x25` | **LINK_PONG** | rx_rssi,seq 2B | 10 | 1 짧은PA | ★PING 응답. HOP=1 |
| `0x30` | RECEIVER_QUERY | 없음 | 8 | 101 | 조회 명령 |
| `0x31` | RECEIVER_REPLY | 16B | 24 | 1 | STATUS_REPORT 와 동일 구조 |
| `0x32` | RECEIVER_BROADCAST | cmd+data | 가변 | 1 | 방송 |
| `0x33` | CMD_SILENCE | passcode 8B | 16 | 101 | 음소거 명령 |
| `0x34` | CMD_TEST | 없음 | 8 | 101 | 시험 명령 |
| `0x35` | CMD_RESET | passcode 8B | 16 | 101 | 리셋 명령 |
| `0x36` | EXT_QUERY | 없음 | 8 | 101 | 대표노드 조회 |
| `0x37` | EXT_GROUP_REPORT | 가변 | 가변 | 1 | 그룹 보고 |
| `0xF0` | DEBUG_PING | — | — | 1 | 디버그 |
| `0xF1` | DEBUG_RSSI | — | — | 1 | 디버그 |

> ⚠ **자주 혼동**: `0x20/0x21` 은 TEST_REQ/TEST_ACK 다. LINK_PING/PONG 은 **0x24/0x25**.
> ⚠ **LINK_PING 은 payload 가 없다.** 헤더에 `rf_pl_link_ping_t{seq}` 구조체가 정의돼
>   있으나 `rf_proto.c:307` 은 `RF_HDR_LEN` 만 보낸다. 정의만 있고 안 쓴다.

### 채널 열의 의미 (`rf_proto.c:389~415`, `#if STEP_N >= 26`)

- **101 (= 100+GROUP)** = wake 채널, 롱PA 12000bit(~4초). 자는 노드를 깨워야 하는 것
  → ALARM 전체 + LINK_PING + QUERY + CMD_* + EXT_QUERY
- **1 (= GROUP)** = 데이터 채널, 짧은PA 80bit. 상시 수신 상대에게 보내는 것 → 나머지 전부

> ★**ch1 과 ch101 은 같은 주파수(447.2752MHz)** 다. 차이는 프리앰블뿐이다.
> **2026-09-04 실측**: ch101 고정 수신 중에 ch1 로 나간 STATUS_REPORT(0x11)가 잡혔다.
> → 롱PA PHY 로 짧은PA 프레임을 받을 수 있다는 뜻. 다만 **1회 관측**이라 확정 아님.
> 반대 방향(ch1 에서 롱PA 수신)은 미확인 — S6b 가 판정한다.

---

## 3. HOP 으로 원본/중계본을 가른다 `buf[7]`

```
HOP=4  원본        (RF_FLOOD_MAX_HOPS, rf_proto.h:267)
HOP=3  1회 중계됨
HOP=2  2회 중계됨
HOP=1  PING/PONG (애초에 중계 안 함) 또는 3회 중계됨
HOP=0  더 이상 중계 안 됨
```

중계는 **알람 영역(0x00~0x0F)만** 대상이다. 중계 시 **SRC 와 SEQ 는 유지**되고 HOP 만 1 줄어든다 (`rf_proto.c:108~110`).

**→ SRC·SEQ 가 같은데 HOP 이 다르면 같은 사건의 중계본이다.**

실측 예 (2026-09-04):
```
#9   08 02 01 03 01 10 00 04 02   SRC=3 SEQ=16 HOP=4  rssi=-12  ← 원본
#12  08 02 01 03 01 10 00 03 02   SRC=3 SEQ=16 HOP=3  rssi=-18  ← 중계본
```
RSSI 가 다르면 **물리적으로 다른 송신기**다. 중계한 노드가 누구인지는 프레임에 안 남는다.

> ⚠ **중계본이 ALARM_STOP 보다 늦게 올 수 있다.** 위 실측에서 #11 STOP 이 tick 53,
> #12 중계본이 tick 60 이었다. 규격상 백오프는 200ms(`RF_FLOOD_BACKOFF_MAX_MS`)인데
> 12초가 걸렸다. 원인 미규명 — LTD_W10D 확인 대상.

---

## 4. 페이로드 상세

### 4.1 ALARM_FIRE (0x01) — cause 1B `buf[8]`

| cause | 이름 | 뜻 |
|---|---|---|
| `0x00` | SMOKE | 연기 자체감지 |
| `0x01` | HEAT | 정온식 자체감지 (감지기 STEP 28, 임시 65℃) |
| `0x02` | MANUAL | **KEY 수동시험** |
| `0x03` | COMBINED | 복합 |
| `0x04` | EMR | **EMR_IO 유선 연동 수신** (감지기 STEP 27) |

> ★**cause 만 보면 "무엇이 울렸나" 를 안다.** 자체감지(0/1)인지, 사람이 눌렀는지(2),
> 옆 감지기에서 유선으로 옮아온 건지(4).
> ⚠ **원발신자만 ALARM_FIRE 를 보낸다.** RF 로 받아서 따라 울린 노드(receiver)는
>   자기 ALARM_FIRE 를 송신하지 않는다 (`app_process.c:263`). 그 노드가 울렸는지는
>   RF 로 직접 확인 불가 — **중계본이 유일한 흔적**이다.

### 4.2 STATUS_REPORT (0x11) / RECEIVER_REPLY (0x31) — 16B

`rf_proto.h:246~257`. **필드 순서 주의: temp_thr 이 smoke_thr 보다 앞이다.**

| offset | 크기 | 필드 | 해석 |
|---|---|---|---|
| 8~9 | u16 | `bat_mv` | 배터리 mV. 1000~4000 정상 |
| 10 | u8 | `flags` | 아래 비트표 |
| 11~12 | u16 | `smoke_raw` | 연기 ADC 12bit 0~4095 |
| 13~14 | **i16** | `temp_c10` | **0.1℃ 단위.** 243 → 24.3℃ |
| 15~16 | u16 | `onset_smoke` | **화재 판정 순간**의 smoke_raw |
| 17~18 | **i16** | `onset_temp` | **화재 판정 순간**의 temp_c10 |
| 19~20 | u16 | `set_temp_thr` | 적용중 온도 임계 (0.1℃) |
| 21~22 | u16 | `set_smoke_thr` | 적용중 연기 임계 (raw) |
| 23 | u8 | `cfg_ver` | CFG_SET 버전. 0=default |

**센티넬(= 값 없음)**: `u16` 은 `0xFFFF`, `i16` 은 `0x8000`(=-32768). `FF FF` 나 `00 80` 이면 **"미측정/미배포"** 이지 0 이 아니다.

**flags 비트** (`rf_proto.h:194~198`)

| bit | 마스크 | 뜻 |
|---|---|---|
| 0 | `0x01` | LOW_BAT 배터리 부족 |
| 1 | `0x02` | SENSOR_ERR 센서 고장 |
| 2 | `0x04` | TEST_MODE 시험 모드 |
| 3 | `0x08` | LINK_WARN 링크 마진 5~10dB |
| 4 | `0x10` | COMM_FAULT 24h 통신 미수신 |

> ★**`onset_*` 이 판정의 핵심이다.** 현재값(`smoke_raw`/`temp_c10`)은 지금 상태고,
> `onset_*` 은 **불이 났다고 판단한 그 순간의 값**이다. 오작동 분석은 여기를 본다.
> `onset_temp = 242`(24.2℃) 인데 `cause=HEAT` 면 **오작동**이다.

### 4.3 짧은 페이로드들

| MSG | 구조 |
|---|---|
| `0x10` HEARTBEAT / `0x21` TEST_ACK | `bat_mv`(u16) + `flags`(u8) |
| `0x12` BATTERY_LOW | `bat_mv`(u16) |
| `0x13` SENSOR_ERROR | `err_code`(u8) |
| `0x15` LINK_QUALITY | `rssi`(i8) + `lqi`(u8) + `margin`(u8) |
| `0x23` SURVEY_REPLY | `rssi`(i8) + `lqi`(u8) + `margin`(u8) + `ngb_cnt`(u8) |
| `0x25` LINK_PONG | `rx_rssi`(i8) + `seq`(u8) ← seq 는 **응답 대상 PING 의 seq** |
| `0x33`/`0x35` CMD_SILENCE/RESET | `passcode`(8B) |

---

## 5. 로그 헤더 읽기 — `ch` / `rssi` / `crc`

**`ch=`** 는 **스니퍼가 듣고 있던 채널**이지 송신 채널이 아니다. ch1/ch101 이 같은 주파수라 교차 수신이 된다 (§2 끝).

**`rssi=`** dBm.

| 값 | 판정 |
|---|---|
| -10 이상 | ⚠ **포화 의심.** 너무 가깝다. 강한데도 복조 실패할 수 있음 |
| -30 ~ -60 | ✅ 양호 |
| -60 ~ -90 | 정상 (거리 있음) |
| -90 이하 | ⚠ 한계. 유실 시작 |

**`crc=`**

- `PASS` → 내용 신뢰 가능
- `FAIL` → **전파는 도달했는데 깨졌다.** 채널/PHY 문제가 아니라 감도·거리·간섭 문제
- **아무 출력 없음** → RF 가 아예 안 온다. **채널 번호부터** 의심

> ★이 3분법이 `FS_S6_CRC_ERR_SHOW=1` 을 켜 둔 이유다. 끄면 `FAIL` 과 `무출력` 이
> 똑같이 무출력으로 보여 원인 분리가 안 된다.

**`[S6a] note evt=N pkt=M`** — ISR 진입 수와 배출 수 불일치. 차이가 계속 벌어지면 수신 처리가 못 따라가는 것.

---

## 6. 실측 판독 예제 (2026-09-04)

### 예제 1 — 화재 발생
```
[S6a] #9 ch=101 rssi=-12 crc=PASS len=9
[S6a]   0000: 08 02 01 03 01 10 00 04 02
```
→ **v1.1 / GROUP 1 / NODE 3 / ALARM_FIRE / SEQ 16 / 원본(HOP=4) / cause=MANUAL**
→ **판정: 3번 감지기에서 사람이 KEY 를 눌러 화재 발신. 자체감지 아님.**

### 예제 2 — 상세 상태
```
[S6a] #10 ch=101 rssi=-11 crc=PASS len=24
[S6a]   0000: 17 02 01 03 11 11 00 04 0A 0C 00 00 00 F3 00 00
[S6a]   0016: 00 F2 00 FF FF FF FF 00
```
```
bat_mv      0A 0C → 3082 mV (3.08V)   정상
flags       00                        이상 없음
smoke_raw   00 00 → 0                 연기 없음
temp_c10    F3 00 → 243               24.3 ℃
onset_smoke 00 00 → 0                 판정 순간 연기 0
onset_temp  F2 00 → 242               판정 순간 24.2 ℃
set_temp_thr  FF FF → 센티넬          임계 미배포
set_smoke_thr FF FF → 센티넬          임계 미배포
cfg_ver     00                        기본 설정
```
→ **판정: 판정 순간이 24.2℃ 상온이고 연기 0. 자체감지 근거가 전혀 없다.**
   **KEY 수동시험이 맞다** (예제 1 의 cause=MANUAL 과 일치).

### 예제 3 — 해제
```
[S6a] #11 ch=101 rssi=-12 crc=PASS len=8
[S6a]   0000: 07 02 01 03 02 12 00 04
```
→ **NODE 3 / ALARM_STOP(0x02) / SEQ 18 / payload 없음** → 경보 해제

### 예제 4 — 중계본
```
[S6a] #12 ch=101 rssi=-18 crc=PASS len=9
[S6a]   0000: 08 02 01 03 01 10 00 03 02
```
→ SRC·SEQ 가 예제 1 과 **동일**한데 HOP 만 4→3, RSSI 도 -12→-18
→ **판정: 다른 노드가 예제 1 을 중계한 것.** 새 화재가 아니다.

---

## 7. 이상 징후 빠른 판별

| 보이는 것 | 의심할 것 |
|---|---|
| `VER` 이 0x02 가 아님 | 오수신(CRC 우연 통과) 또는 구버전 감지기 |
| `SRC=0` 인데 송신됨 | Setting mode 는 발신 금지인데 나옴 = 감지기 버그 |
| `GRP` 가 기대와 다름 | 다른 그룹 신호를 줍고 있음. 같은 주파수 인접 그룹 |
| `cause=HEAT` 인데 `onset_temp` 가 상온 | **오작동** 또는 임계 설정 오류 |
| `cause=SMOKE` 인데 `onset_smoke=0` | **오작동** 또는 센서 이상 |
| `flags` bit1(SENSOR_ERR) 세팅 | 센서 고장. 알람 신뢰도 낮음 |
| 같은 SEQ 가 HOP 만 다르게 반복 | 정상 (중계). 새 사건 아님 |
| SEQ 가 건너뜀 | 유실 있음. RSSI·거리 확인 |
| `ALARM_STOP` 뒤에 `ALARM_FIRE` 중계본 | 늦은 중계. §3 경고 참조 |
| `crc=FAIL` 만 계속 | 전파는 오는데 깨짐 → 감도/거리/간섭 |
| 출력이 딱 1개 후 멈춤 | **RX 재무장 실패.** `[S6a] rx_transitions ... OK` 확인 |

---

## 8. 참조

| 문서 | 용도 |
|---|---|
| `../LTD_W10D/Software/LTD_W10D_V03/rf_proto.h` | **단일 기준.** MSG/페이로드/상수 |
| `../LTD_W10D/Software/LTD_W10D_V03/rf_proto.c:389~415` | 채널 매핑 (`pick_channel`) |
| `Software/NetAnalyzer_FG23_V01/step_config.h` | 스텝 계획 + 실측 이력 |
| `../RF_STEP구현_SYNC.md` | LTD_W10D 협업 창구 (⚠ MSG 값 오기 있음, 헤더 우선) |
| `../Network_Analyzer/FG23_UART_인터페이스.md` | UART 프레임·CSV 스펙 |
