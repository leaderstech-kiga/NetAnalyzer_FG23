# 작업 재개 메모 — NetAnalyzer_FG23 (FG23 스니퍼 펌웨어)

> **최종 작업일**: 2026-07-31
> **재개 지점**: **S7(v1.1 파서) 착수** — 아무것도 안 기다리고 바로 가능
> **판단 기준**: `../../JUDGMENT_V1.1.md` (§3.12 스텝 기반 개발 규율 적용 중)
> **협업 창구**: `../RF_STEP구현_SYNC.md` (LTD_W10D 세션과 공유)

---

## 0. 한 줄 요약

Phase 1(FG23↔PC UART) 완료 + S5(RAIL 채널 유효성) 통과.
다음은 **S7 파서**(즉시 가능) 이고, S6a·S9 는 **아래 §4 결정 2건**에 막혀 있다.

---

## 1. 지금 상태

### 완료 (전부 실측 검증 + 앵커 보관)

| 스텝 | 내용 | 실측 결과 |
|---|---|---|
| S1 | UART 출력 | 배너 + 1초 틱 |
| S2 | CRC-16/CCITT-FALSE | 0x29B1 / 0xF508 |
| S3a | 560B 프레임 조립 | 스펙 예제 CHK=EB7D 재현 |
| S3b | 논블로킹 TX (링버퍼+폴링) | 호출 반환 48,611us → 183~213us |
| S4a | 프레임 수신·재동기 | 3종 + 헤더 사전검사 강화 |
| S4b | VER/STAT + ACK | 5/5 |
| S4c | CH/PHY/MODE/SCAN | 16/16 |
| — | 보율 115200 → **921600** | 24개 검사 전부 재통과, 프레임 손상 0 |
| S5 | RAIL init + 채널 유효성 | **8/8** (ch 0/24/100/124 유효, 25/50/99/125 무효) |

**누적 자동 판정 24개 + S5 8개. 앵커 10개** (`Software/_known_good/`).

### 현재 매크로 (`Software/NetAnalyzer_FG23_V01/step_config.h`)

```c
#define FS_N               5    /* 스텝 (1~10) — 낮추면 그 이전 상태로 롤백 */
#define FS_TX_NONBLOCKING  1    /* 0 = 블로킹 TX (S3a known-good) 로 복귀 */
#define FS_S4              3    /* 1=S4a만 / 2=+S4b / 3=+S4c */
```

### git

```
54fae03  FG23 스니퍼 Phase 1 완료 — UART 프레임 송수신·명령 검증 (S1~S4c)
5f85102  Initial commit
```

**⚠ 미커밋**: S5 구현(`app_init.c`), `step_config.h` 이력, `_known_good/S5_20260731.s37`,
배너 하드코딩 보율 정정. → **재개 시 S5 커밋부터** 하는 게 깔끔하다.

---

## 2. 빌드·검증 방법

**빌드**: Simplicity Studio → Refresh(F5) → 빌드 → 플래시. (빌드는 항상 Studio)

**PC 시험** (`Software/_tools/`, 터미널·스크립트 **보율 921600**):

```bash
pip3 install pyserial                                  # 최초 1회
ls /dev/tty.usb*                                       # 포트 확인 (현재 usbserial-0001)

python3 fg23_send.py /dev/tty.usbserial-0001           # 프레임 3종 (정상/CRC깨짐/절단)
python3 fg23_send.py /dev/tty.usbserial-0001 --s4b     # ACK 왕복 5종
python3 fg23_send.py /dev/tty.usbserial-0001 --s4c     # 상태명령 16종
python3 fg23_send.py --selftest                        # 시리얼 없이 도구 자가검증
python3 fg23_rxsim.py                                  # 수신 상태기 시뮬레이션(실기 예측)
```

**부팅 출력만 볼 때**: 터미널 921600 8-N-1 로 열고 리셋. S2/S3a/S3b/S5 결과가 순서대로 나온다.

**회귀 시**: `step_config.h` 매크로를 낮추거나, `_known_good/*.s37` 을 Commander 로 직접 플래시.

---

## 3. 다음 작업 — S7 (즉시 가능)

**S7 = v1.1 헤더 파서 → CSV 13필드.** 감지기 없이 검증 가능하다.

`../RF_STEP구현_SYNC.md §3.1` 에 **LTD_W10D 가 실제로 관측한 프레임 바이트열**이 있다:

```
0x17 | 02 01 01 11 04 00 04 | a7 0b 00 0000 0080 0000 0080 ffff ffff 00
 len    VER GRP SRC MSG SEQ  HOP   STATUS_REPORT payload 16B
```

- `0x17` = 23 = 헤더 7B + 페이로드 16B (일치 확인함)
- 기대 디코드: `bat_mv=2983`, `smoke_raw=0`, `temp_c10=sentinel(-32768)`,
  `onset_smoke=0`(sentinel 아님 = KEY MANUAL 시점 캡처됨), `set_*_thr=0xFFFF`(미배포), `cfg_ver=0`

→ 이 바이트열을 넣어 CSV 13필드가 정확히 나오면 **S7 완료조건 충족**.
   페이로드 상세 구조는 SYNC §3.1 표 참조.

---

## 4. ★막혀 있는 것 — 결정/정보 2건

### (1) 감지기 그룹 번호  → S6a 를 여는 유일한 열쇠

S6a = wake 채널(ch100) 실수신. **감지기 그룹 번호만 알면 착수 가능**하다.

- SYNC §3.1 트리거 1 = *"부팅 자가진단 완료 후 자동 1회 STATUS_REPORT"*
  → **감지기 리셋만으로 프레임 1개 발생.** 주기 송신을 기다릴 필요 없다.
- 들을 채널 = **감지기 채널 − 1** (§5 참조). 그룹 1 이면 스니퍼 ch100(롱PA).

### (2) TX 순서 앞당김 여부  → 소장 판단 필요 (안전 관련)

LTD_W10D 세션이 **"Network 가 QUERY 발신 준비 완료를 알리면 STEP 15 시험 진행"** 으로
대기 중이다(SYNC §6). 우리 TX 는 계획상 S9 이고, `step_config.h` 에
*"FS_N<9 구간은 TX 경로 컴파일 제외"* 로 안전 정책을 박아 뒀다.

**앞당길 근거**: `RECEIVER_QUERY(0x30)` 는 조회 명령이라 경보·연동을 방해하지 않는다.
절대선이 막는 것은 알람 중 능동 송신, `CMD_SILENCE/RESET` 같은 권한 명령이다.
**QUERY 하나만 열고** SURVEY·PING·CFG_SET·SILENCE·RESET 는 계속 닫아둘 수 있다.

**앞당기면 한 번에 풀린다**:
```
스니퍼 QUERY 발신 → 감지기 수신 → STATUS_REPORT 응답 → 스니퍼 수신
  [우리 TX 검증]     [LTD_W10D STEP 15 검증]      [우리 S6/S7 검증]
```
수동으로 기다리는 대신 **우리가 원할 때 트리거하는 송신원**이 생긴다.

→ 안전 정책 변경이라 임의로 진행하지 않고 대기 중.

---

## 5. ★잊으면 안 되는 발견들

### 채널 번호가 양측에서 1칸 어긋나 있다

`autogen/rail_config.c` 실측:

| | start~end | offset | f(ch1) |
|---|---|---|---|
| 감지기 V03 | 1~25 (1그룹) | 1 | 447.2627 MHz |
| 스니퍼(협력업체 원본) | 0~24 / 100~124 | 0 / 100 | 447.2752 MHz |

스펙(`../Network_Analyzer/CLAUDE.md §7.1`)은 `f(g)=447.2627+g×0.0125` →
**스니퍼가 스펙과 맞고 V03 감지기가 한 칸 낮다.**

**운용 규칙 (STEP 26 해소 전까지)**: `스니퍼 ch = 감지기 채널 − 1` (wake 는 +100)
- 감지기 그룹1(ch1) → 스니퍼 **ch0**(짧은PA) 또는 **ch100**(롱PA)

### 스니퍼 ch0 과 ch100 은 같은 주파수다

둘 다 447.2627 MHz. 차이는 PHY 의 프리앰블 기대값뿐(80bit vs 12000bit).
→ **S6a/S6b 가 주파수를 안 바꾸고 PHY 만 바꾸는 단일변수 시험이 된다.**
   안 잡히면 원인이 "주파수"가 아니라 무조건 "프리앰블 불일치"로 좁혀진다.

### 감지기는 지금 9000bit all-long 으로 송신 중

STEP 26(2그룹 복원) 이전 상태. 스니퍼 ch100 기대치가 12000bit 이므로
**12000 ≥ 9000 → S6a 는 잡혀야 정상**. ch0(80bit 기대)은 미확인 → S6b.

### 스펙 문서에 아직 안 들어간 것 (Cowork 반영 대상)

`../Network_Analyzer/FG23_UART_인터페이스.md` 에 추가 필요:
1. **ACK(TYPE='A') DATA 포맷** — `cmd(16),result(4),detail` (우리가 정하고 구현·검증 완료)
2. **CH 유효범위** = radioconf 실채널 `0~24, 100~124` (25~99 미존재, S5 에서 라디오가 확인)
3. **§1.1 vs §3.1 모순 처리** — ch 가 PHY 를 인코딩하므로 모순되는 PHY 명령은 ERR

### 반복해서 물린 함정

- **생성물(autogen)까지 봐야 "확인"이다** — SDK 소스·템플릿만 보고 판단했다가
  `EUSART0_TX_IRQHandler` 중복 정의로 링크 실패(S3b). 템플릿은 매크로라 grep 을 피해간다.
- **무효한 방어코드를 넣고 안심하는 게 제일 위험하다** — 헤더 사전검사 1차 시도가
  재동기 경로를 안 타서 무효였는데 시뮬레이터가 잡아냈다(S4a+).
- **시험 도구도 자가검증이 필요하다** — `fg23_send.py` 자가검증이 스크립트 자신의
  LEN 계산 버그를 먼저 잡았다.
- **기능을 넣으면 기존 회귀 세트의 "미구현 기대"도 같이 손봐야 한다** — S4c 구현 후
  `--s4b` 의 `CH 3` 기대값이 낡아 오경보를 냈다.
- **로그가 설정과 어긋나면 안 된다** — `RING+IRQ`(실제는 폴링), `UART 115200`(실제 921600)
  두 번 발생. 하드코딩을 없애고 실제 값을 출력하도록 고쳤다.

---

## 6. 참조

| 문서 | 용도 |
|---|---|
| `../RF_STEP구현_SYNC.md` | **LTD_W10D 협업 창구** — 재개 시 먼저 읽을 것 |
| `../Network_Analyzer/FG23_UART_인터페이스.md` | UART 프레임·CSV·명령 스펙 (단일 기준) |
| `../Network_Analyzer/RF_동작_상세정의_WORKING.md` | RF 스펙 (§7 디코드 · §G 채널) |
| `../Network_Analyzer/1차작업_검증계획_5단계.md` | Phase 1~5 검증 계획 |
| `Software/NetAnalyzer_FG23_V01/step_config.h` | **스텝 계획 + 전 실측 이력 + 보류 결정** |
| `Software/_known_good/README.md` | 앵커 목록·롤백 절차 |
