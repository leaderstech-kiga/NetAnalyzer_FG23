# 작업 재개 메모 — NetAnalyzer_FG23 (FG23 스니퍼 펌웨어)

> **최종 작업일**: 2026-09-04
> **재개 지점**: **S6a RX 재무장 결함 수정** (S6a 자체는 ★PASS)
> [원본 -- 2026-07-31: "재개 지점 = S7(v1.1 파서) 착수". §4 결정 2건이
>  2026-09-02~09-03 에 해소돼 S6a 가 열렸다.]

---

## ★재개하면 여기부터 (2026-09-03 작성)

### 딱 3가지만 기억하면 된다

1. **S6a 는 통과했다** (2026-09-04). ALARM_FIRE 를 ch101 에서 실제로 받았다.
   앵커 `_known_good/S6a_20260904.s37`. **ch101 은 이제 예측이 아니라 실측 확정.**
2. **★다음 할 일 = RX 자동 재무장 수정.** 지금 펌웨어는 패킷 1개 받으면
   라디오가 IDLE 로 떨어져 그 뒤로 못 받는다 (화재 10회 중 1회 수신).
3. **rssi=-6dBm 은 너무 세다** (정상 -40~-80 = 포화 영역). 재무장 고친 뒤에도
   수신율이 낮으면 **거리를 벌려서** 재측정할 것.

### 다음 작업 상세 — RX 재무장

**원인**: `config/sl_rail_util_init_inst0_config.h:161,:166`
```
SL_RAIL_UTIL_INIT_TRANSITION_INST0_RX_SUCCESS  RAIL_RF_STATE_IDLE
SL_RAIL_UTIL_INIT_TRANSITION_INST0_RX_ERROR    RAIL_RF_STATE_IDLE
```
수신 성공 후 IDLE 로 전이되는데 `app_init.c` 의 S6a 블록에 재무장이 없다.
감지기는 같은 함정을 이미 겪었다 — `LTD_W10D_V03/drv_rf.c:158~161`
*"명시적 재활성 없으면 이후 알람 수신 불가"*.

**수정 방향** (둘 다 넣는다 — 스니퍼는 놓치면 안 되는 장비):
```c
/* (a) 자동 전이를 RX 유지로 — 패킷 사이 공백 없음 */
sl_rail_state_transitions_t tr = { .success = SL_RAIL_RF_STATE_RX,
                                   .error   = SL_RAIL_RF_STATE_RX };
sl_rail_set_rx_transitions(rail, &tr);      /* sl_rail.h:2390 */

/* (b) fallback — s6_rx_poll 배출 후 start_rx 재호출 */
```
Studio config 를 직접 고치는 방법도 있으나, **Studio 재생성이 설정을 되돌린
사고가 이미 있었으므로**(board_cfg.h:38) 코드에서 명시하는 쪽이 안전하다.

**검증**: 화재 10회 발생 → **10회 수신**되면 완료. (지금은 1/10)

### [원본 -- 2026-09-03 작성, S6a 통과로 무효]
> 1. S6a 코드는 다 짰다. 근데 한 번도 빌드 안 했다. Studio 여는 게 첫 일.
> 2. 미커밋 4개 파일이 있다. 빌드 통과 확인 전이라 일부러 안 커밋했다.
> 3. `ch101` 은 예측값이다. 안 잡히면 그게 1순위 용의자다 (프리앰블 아님).

### 시험 절차 (그대로 따라하면 됨)

```
1) Simplicity Studio → Refresh(F5) → 빌드
   ⚠ 빌드 에러 날 수 있다. 미검증 코드다. 에러 로그 그대로 Claude 에 주면 됨.
2) 플래시
3) 터미널 921600 8-N-1 열고 스니퍼 리셋
   → 기대: [S5] result: 8/8 PASS
           [S6a] start_rx ch=101 OK  (crc_err_show=1)
           [S6a] listening...
4) 감지기 DIP 확인 -- GROUP=1 / NODE=1
   ⚠⚠ NODE=0 이면 Setting mode = RF 발신 전면 skip. 아무것도 안 나온다.
5) 감지기 리셋
   → 기대: [S6a] #1 ch=101 rssi=-XX crc=PASS len=8
           [S6a]   0000: 07 02 01 01 24 XX XX 01
                          |  |  |  |  |  |__|  |__ HOP=0x01
                          |  |  |  |  |_____ SEQ 2B little-endian
                          |  |  |  |__ MSG  = 0x24 LINK_PING
                          |  |  |__ SRC = NODE = 0x01
                          |  |__ GRP = GROUP = 0x01
                          |__ VER = 0x02 (v1.1)   ← LEN=0x07 은 buf[0]
   → crc=PASS 나오면 **S6a 통과**. 앵커 뜨고 커밋.

   ★프레임별 기대 길이 (RAIL 은 wire 그대로 → buf[0]=LEN, 본문은 buf[1]~)
     LINK_PING     0x24  LEN=0x07 (헤더 7B)          → len=8
     ALARM_FIRE    0x01  LEN=0x08 (헤더 7B + cause)  → len=9
     STATUS_REPORT 0x11  LEN=0x17 (헤더 7B + 16B)    → len=24
   근거: rf_proto.h:32(VER=0x02) :37(HDR_LEN=7), SYNC §3.1 실관측 바이트열
```

### 안 나올 때 — 이 순서로 좁혀라 (순서가 중요하다)

| 증상 | 판정 | 다음 |
|---|---|---|
| `start_rx ch=101 FAIL` | 수신 무장 자체 실패 | 전파 문제 아님. 채널/PHY 설정부터 |
| `crc=FAIL` 이라도 뜸 | **RF 는 도달했다** | 감도/거리/간섭 쪽 |
| 아무 출력 없음 | RF 가 안 온다 | ★**채널 번호부터** — `ch100` 도 시험해 볼 것 |
| 그래도 없음 | | 그 다음이 PHY/프리앰블 |

> ★`FS_S6_CRC_ERR_SHOW 1` 로 둔 이유가 이 표다. 0 이면 2행과 3행이
> 똑같이 "무출력"으로 보여 원인 분리가 안 된다.

### 통과 후

- 앵커 저장: `_known_good/S6a_<날짜>.s37` + `_known_good/README.md` 1줄
- `step_config.h` 실측 이력에 1줄
- **커밋** (★스텝 단위로 분리할 것 — 65b37d5 처럼 합치지 말 것)
- 다음: **S6b** = `FS_S6_CHANNEL` 을 `101` → `1` 로 **딱 이 값만** 바꿔 재빌드.
  주파수는 그대로(447.2752MHz)고 PHY 만 바뀐다 = 단일변수 시험.
  ⚠ 판정은 **STATUS_REPORT(0x11)** 로. LINK_PONG 은 감지기 2대 있어야 나온다.
- 그 다음: **S7 파서** (감지기 없이 오프라인 검증 가능, §3 참조)

### 지금 미커밋 상태 (2026-09-03)

```
M app_init.c        S6a RX 무장 (config_events / config_rx_options / start_rx)
M app_process.c     S6a ISR hold + 메인루프 s6_rx_poll() hex 덤프
M step_config.h     FS_N 5->6, FS_S6_CHANNEL 101, FS_S6_CRC_ERR_SHOW 1
M 작업재개_메모.md   §0/§1/§3/§4/§5 낡은 전제 정정 + 이 블록
```

**빌드 통과 전이라 일부러 안 커밋했다.** 회귀 시 `FS_N` 을 5 로 내리면
RF 경로가 통째로 컴파일에서 빠지고, 그게 안 되면
`_known_good/S5_20260731.s37` 을 Commander 로 직접 플래시하면 된다.

### 안전 — 이번 스텝은 절대선 충돌 없음

S6a 는 **수신 전용**이다. 송신 API 0줄, `FS_N<9` TX 컴파일 제외 정책 그대로.
소장 결정(2026-09-02 옵션 A) = 수동 관측, QUERY TX 앞당기지 않음.

### 남은 TODO (급하지 않음)

- `../RF_STEP구현_SYNC.md:297,298,307` LINK_PING=0x20/PONG=0x21 **오기 정정**
  (실값 0x24/0x25). 협업 창구 문서라 LTD_W10D 세션과 조율 필요.
- 65b37d5 커밋 메시지가 `커밋 1 — S5 (코드)` 로 남음. amend + force-push 필요.
- §5 "스펙 문서에 아직 안 들어간 것" 3건 Cowork 반영 (ACK 포맷 / CH 유효범위 / §1.1 vs §3.1)

---
> **판단 기준**: `../../JUDGMENT_V1.1.md` (§3.12 스텝 기반 개발 규율 적용 중)
> **협업 창구**: `../RF_STEP구현_SYNC.md` (LTD_W10D 세션과 공유)

---

## 0. 한 줄 요약

Phase 1(FG23↔PC UART) 완료 + S5(RAIL 채널 유효성) 통과.
**§4 결정 2건 모두 해소** → 다음은 **S6a(wake ch101 실수신)**.
S7 파서는 감지기 없이 병행 가능. S9(TX) 는 계획대로 계속 닫아 둔다.

[원본 -- 2026-07-31: "다음은 S7 파서(즉시 가능) 이고, S6a·S9 는 아래 §4
 결정 2건에 막혀 있다."]

---

## 1. 지금 상태

### 완료 (전부 실측 검증 + 앵커 보관)

| 스텝 | 내용 | 실측 결과 |
|---|---|---|
| S1 | UART 출력 | 배너 + 1초 틱 |
| S2 | CRC-16/CCITT-FALSE | 0x29B1 / 0xF508 |
| S3a | 560B 프레임 조립 | 스펙 예제 CHK=EB7D 재현 |
| S3b | 논블로킹 TX (링버퍼+폴링) | 호출 반환 48,611us → 183~213us |
| S4a | 프레임 수신·재동기 | 3종 + 헤더 사전검사 강화 |
| S4b | VER/STAT + ACK | 5/5 |
| S4c | CH/PHY/MODE/SCAN | 16/16 |
| — | 보율 115200 → **921600** | 24개 검사 전부 재통과, 프레임 손상 0 |
| S5 | RAIL init + 채널 유효성 | **8/8** (ch 0/24/100/124 유효, 25/50/99/125 무효) |

**누적 자동 판정 24개 + S5 8개. 앵커 10개** (`Software/_known_good/`).

### 현재 매크로 (`Software/NetAnalyzer_FG23_V01/step_config.h`)

```c
#define FS_N               5    /* 스텝 (1~10) — 낮추면 그 이전 상태로 롤백 */
#define FS_TX_NONBLOCKING  1    /* 0 = 블로킹 TX (S3a known-good) 로 복귀 */
#define FS_S4              3    /* 1=S4a만 / 2=+S4b / 3=+S4c */
```

### git

```
65b37d5  S5 (코드) + S6 전제 정정 — 2026-09-03 커밋, origin/main push 완료
54fae03  FG23 스니퍼 Phase 1 완료 — UART 프레임 송수신·명령 검증 (S1~S4c)
5f85102  Initial commit
```

**미커밋 없음** (2026-09-03 기준). 65b37d5 에 S5 구현 + `step_config.h` §S6
전제 정정 + `_known_good/S5_20260731.s37` 앵커 + 이 메모가 모두 들어갔다.

> ⚠ 65b37d5 는 커밋 메시지 작성 실수로 **두 커밋 분량이 한 커밋에 합쳐졌고**
> 제목이 `커밋 1 — S5 (코드)` 로 남아 있다. push 완료 상태라 고치려면
> amend + force-push 필요. 회귀 시 로그로 bisection 하는 구조라 **다음부터는
> 스텝 단위로 분리 커밋**할 것.

[원본 -- 2026-07-31: "⚠ 미커밋: S5 구현(app_init.c), step_config.h 이력,
 _known_good/S5_20260731.s37, 배너 하드코딩 보율 정정.
 → 재개 시 S5 커밋부터 하는 게 깔끔하다." → 2026-09-03 완료됨]

---

## 2. 빌드·검증 방법

**빌드**: Simplicity Studio → Refresh(F5) → 빌드 → 플래시. (빌드는 항상 Studio)

**PC 시험** (`Software/_tools/`, 터미널·스크립트 **보율 921600**):

```bash
pip3 install pyserial                                  # 최초 1회
ls /dev/tty.usb*                                       # 포트 확인 (현재 usbserial-0001)

python3 fg23_send.py /dev/tty.usbserial-0001           # 프레임 3종 (정상/CRC깨짐/절단)
python3 fg23_send.py /dev/tty.usbserial-0001 --s4b     # ACK 왕복 5종
python3 fg23_send.py /dev/tty.usbserial-0001 --s4c     # 상태명령 16종
python3 fg23_send.py --selftest                        # 시리얼 없이 도구 자가검증
python3 fg23_rxsim.py                                  # 수신 상태기 시뮬레이션(실기 예측)
```

**부팅 출력만 볼 때**: 터미널 921600 8-N-1 로 열고 리셋. S2/S3a/S3b/S5 결과가 순서대로 나온다.

**회귀 시**: `step_config.h` 매크로를 낮추거나, `_known_good/*.s37` 을 Commander 로 직접 플래시.

---

## 3. 병행 작업 — S7 (감지기 없이 즉시 가능)

> 2026-09-03: **주작업은 S6a 로 바뀌었다** (§4 해소). S7 은 S6a 빌드·플래시를
> 기다리는 동안 병행할 항목. 파서가 준비돼 있으면 S6a 실측 로그를 hex 대신
> CSV 13필드로 바로 읽어 원인 좁히기가 빨라진다.
> ⚠ 단 **S7 을 S6a 앞에 끼워넣지 말 것** — S6a 판정에 파서 변수가 섞인다.
>   (원칙 2 — 한 번에 한 변수)
> [원본 제목 -- "## 3. 다음 작업 — S7 (즉시 가능)"]

**S7 = v1.1 헤더 파서 → CSV 13필드.** 감지기 없이 검증 가능하다.

`../RF_STEP구현_SYNC.md §3.1` 에 **LTD_W10D 가 실제로 관측한 프레임 바이트열**이 있다:

```
0x17 | 02 01 01 11 04 00 04 | a7 0b 00 0000 0080 0000 0080 ffff ffff 00
 len    VER GRP SRC MSG SEQ  HOP   STATUS_REPORT payload 16B
```

- `0x17` = 23 = 헤더 7B + 페이로드 16B (일치 확인함)
- 기대 디코드: `bat_mv=2983`, `smoke_raw=0`, `temp_c10=sentinel(-32768)`,
  `onset_smoke=0`(sentinel 아님 = KEY MANUAL 시점 캡처됨), `set_*_thr=0xFFFF`(미배포), `cfg_ver=0`

→ 이 바이트열을 넣어 CSV 13필드가 정확히 나오면 **S7 완료조건 충족**.
   페이로드 상세 구조는 SYNC §3.1 표 참조.

---

## 4. ★해소됨 — 결정/정보 2건 (2026-09-02~09-03)

> **둘 다 풀렸다. 막혀 있는 것 없음.** 원문은 각 항목 아래 보존.

### (1) 감지기 그룹 번호 → ★해소 (2026-09-03)

**GROUP=1 / NODE=1 로 합의됨** (`../RF_STEP구현_SYNC.md:287~291`).
들을 채널은 **감지기 채널 − 1 이 아니라 그대로**다 (§5 정정 참조).

- wake **ch101** (롱PA 12000bit) ← LINK_PING(0x24) / ALARM_FIRE(0x01)
- 데이터 **ch1** (짧은PA 80bit) ← STATUS_REPORT(0x11) / LINK_PONG(0x25)
- ⚠ **NODE=0 AND GROUP=1 = Setting mode = RF 발신 전면 skip**
  (감지기 STEP 29a, `rf_proto.c:284~288` / `:475~479`) → 시료 DIP 는 **NODE=1**
- ⚠ **LINK_PONG 은 타 노드 PING 이 있어야 발생** → 시료 1대면 미관측이 정상.
  S6b 판정은 **STATUS_REPORT 로 하라.** PONG 기다리지 말 것.
- ⚠ `SYNC:297,298,307` 의 LINK_PING=0x20 / PONG=0x21 은 **오기**.
  실값 **0x24/0x25** (`rf_proto.h:135,136`), 0x20/0x21 은 TEST_REQ/TEST_ACK
  (`rf_proto.h:131,132`). **헤더를 기준**으로 쓸 것. (SYNC 정정 미반영 — TODO)
- 트리거는 그대로: **감지기 리셋 1회로 LINK_PING + STATUS_REPORT 발생.**
- ★**ch101/ch1 은 감지기 소스에서 유도한 예측값이며 실수신 미확인.**
  S6a 가 이 예측을 판정하는 시험이다. 통과 전까지 "확인됨" 으로 옮겨 적지 말 것.

[원본 -- 2026-07-31, 무효화됨. 보존]
> ### (1) 감지기 그룹 번호  → S6a 를 여는 유일한 열쇠
> S6a = wake 채널(ch100) 실수신. **감지기 그룹 번호만 알면 착수 가능**하다.
> - SYNC §3.1 트리거 1 = *"부팅 자가진단 완료 후 자동 1회 STATUS_REPORT"*
>   → **감지기 리셋만으로 프레임 1개 발생.** 주기 송신을 기다릴 필요 없다.
> - 들을 채널 = **감지기 채널 − 1** (§5 참조). 그룹 1 이면 스니퍼 ch100(롱PA).

### (2) TX 순서 앞당김 여부 → ★결정됨: **앞당기지 않는다** (소장, 2026-09-02)

**옵션 A 채택** (`../RF_STEP구현_SYNC.md:259`) = **수동 관측**으로 간다.
감지기 재플래시 + 부팅 자가진단 프레임을 스니퍼가 받는 방식.

→ **우리 TX 는 계획대로 S9 까지 컴파일 제외 유지.** QUERY 도 열지 않는다.
   `step_config.h` 의 "FS_N<9 구간 TX 경로 컴파일 제외" 안전 정책 **그대로**.
   S6a 는 RX 전용이라 절대선 충돌 없음.
→ LTD_W10D 쪽 STEP 15(QUERY 응답) 시험은 계속 대기 상태.
→ 아래 검토 내용은 **나중에 다시 꺼낼 때를 위해 보존**한다.

[원본 판단 근거 -- 2026-07-31 작성. 결정은 "앞당기지 않음"으로 났으나 논거는 유효]

LTD_W10D 세션이 **"Network 가 QUERY 발신 준비 완료를 알리면 STEP 15 시험 진행"** 으로
대기 중이다(SYNC §6). 우리 TX 는 계획상 S9 이고, `step_config.h` 에
*"FS_N<9 구간은 TX 경로 컴파일 제외"* 로 안전 정책을 박아 뒀다.

**앞당길 근거**: `RECEIVER_QUERY(0x30)` 는 조회 명령이라 경보·연동을 방해하지 않는다.
절대선이 막는 것은 알람 중 능동 송신, `CMD_SILENCE/RESET` 같은 권한 명령이다.
**QUERY 하나만 열고** SURVEY·PING·CFG_SET·SILENCE·RESET 는 계속 닫아둘 수 있다.

**앞당기면 한 번에 풀린다**:
```
스니퍼 QUERY 발신 → 감지기 수신 → STATUS_REPORT 응답 → 스니퍼 수신
  [우리 TX 검증]     [LTD_W10D STEP 15 검증]      [우리 S6/S7 검증]
```
수동으로 기다리는 대신 **우리가 원할 때 트리거하는 송신원**이 생긴다.

→ 안전 정책 변경이라 임의로 진행하지 않고 대기 중.

---

## 5. ★잊으면 안 되는 발견들

### ★채널은 양측이 완전히 같다 (2026-09-03 정정 — 이전 서술 무효)

**`autogen/rail_config.c` 가 양측 바이트 단위 동일** (md5 `13ae6b37…`, 424줄).
감지기 STEP 26(2026-08-13, 2채널 dual-PHY 복원) 으로 해소됐다.

| | start~end | offset | 프리앰블 |
|---|---|---|---|
| 감지기 V03 `:376~380` | 0~24 | 0 | 80bit (짧은PA) |
| 감지기 V03 `:393~397` | 100~124 | 100 | 12000bit (롱PA) |
| 스니퍼 `:376~380` | 0~24 | 0 | 80bit (짧은PA) |
| 스니퍼 `:393~397` | 100~124 | 100 | 12000bit (롱PA) |

프리앰블 근거: 양측 `config/rail/radio_settings.radioconf` **같은 라인**
`:48~49`=80 / `:115~116`=12000. base 447262700 / spacing 12500 도 동일.

**운용 규칙: 보정 없음. `스니퍼 ch = 감지기 ch` 그대로.**
- 감지기 GROUP=1 → 데이터 **ch1**(짧은PA) / wake **ch101**(롱PA)
- 근거: `LTD_W10D_V03/rf_proto.c:389~415` (`#if STEP_N >= 26`),
  ALARM·LINK_PING·QUERY·CMD_* → `100+g`, 그 외 → `g`.
  감지기 `step_config.h:73` = `STEP_N 29` → 이 분기 활성.

> ⚠ **여기가 S6a 실패 시 1순위 용의자다.** 아래 [원본] 의 `g-1` 규칙을 쓰면
> 스니퍼는 **ch100** 을 듣는데 감지기는 **ch101** 로 쏜다. 12.5kHz 어긋나고
> BW 13kHz 라 못 잡는다. → 프리앰블보다 **채널 번호를 먼저** 의심할 것.

<details><summary>[원본 -- 2026-07-31 작성, STEP 26 으로 무효화됨. 보존]</summary>

```
### 채널 번호가 양측에서 1칸 어긋나 있다

`autogen/rail_config.c` 실측:

| | start~end | offset | f(ch1) |
| 감지기 V03 | 1~25 (1그룹) | 1 | 447.2627 MHz |
| 스니퍼(협력업체 원본) | 0~24 / 100~124 | 0 / 100 | 447.2752 MHz |

스펙(`../Network_Analyzer/CLAUDE.md §7.1`)은 `f(g)=447.2627+g×0.0125` →
**스니퍼가 스펙과 맞고 V03 감지기가 한 칸 낮다.**

**운용 규칙 (STEP 26 해소 전까지)**: `스니퍼 ch = 감지기 채널 − 1` (wake 는 +100)
- 감지기 그룹1(ch1) → 스니퍼 **ch0**(짧은PA) 또는 **ch100**(롱PA)
```
</details>

### ch1 과 ch101 은 같은 주파수다 (단일변수 시험 성립)

`f = 447.2627MHz + 12.5kHz × (ch − offset)` →
ch1 = 447.2627 + 12.5k×(1−0) = **447.2752 MHz**,
ch101 = 447.2627 + 12.5k×(101−100) = **447.2752 MHz**. 같다.
차이는 PHY 프리앰블 기대값뿐(80bit vs 12000bit).
→ **S6a/S6b 가 주파수를 안 바꾸고 PHY 만 바꾸는 단일변수 시험이 된다.**
   ch101(S6a) 은 잡히는데 ch1(S6b) 이 안 잡히면 원인은 "주파수"가 아니라
   무조건 "프리앰블 불일치"로 좁혀진다.
   ⚠ 단, **둘 다** 안 잡히면 주파수/채널 번호 자체를 의심해야 한다 (위 함정).

> [원본 -- ch0/ch100 기준 서술이었음. 채널이 1칸 올라가 ch1/ch101 로 정정.
>  "둘 다 같은 주파수" 라는 성질 자체는 그대로 유효.]

### ★감지기는 dual-PHY 로 송신한다 (2026-09-03 정정 — "9000bit all-long" 무효)

STEP 26 이후 **데이터 80bit / wake 12000bit** = 스니퍼 기대값과 **완전히 일치**.
→ S6a(ch101 롱PA)·S6b(ch1 짧은PA) 모두 **프리앰블 불일치 없음**.

<details><summary>[원본 -- 2026-07-31 작성, STEP 26 으로 무효화됨. 보존]</summary>

```
### 감지기는 지금 9000bit all-long 으로 송신 중

STEP 26(2그룹 복원) 이전 상태. 스니퍼 ch100 기대치가 12000bit 이므로
**12000 ≥ 9000 → S6a 는 잡혀야 정상**. ch0(80bit 기대)은 미확인 → S6b.
```
</details>

### 스펙 문서에 아직 안 들어간 것 (Cowork 반영 대상)

`../Network_Analyzer/FG23_UART_인터페이스.md` 에 추가 필요:
1. **ACK(TYPE='A') DATA 포맷** — `cmd(16),result(4),detail` (우리가 정하고 구현·검증 완료)
2. **CH 유효범위** = radioconf 실채널 `0~24, 100~124` (25~99 미존재, S5 에서 라디오가 확인)
3. **§1.1 vs §3.1 모순 처리** — ch 가 PHY 를 인코딩하므로 모순되는 PHY 명령은 ERR

### 반복해서 물린 함정

- **생성물(autogen)까지 봐야 "확인"이다** — SDK 소스·템플릿만 보고 판단했다가
  `EUSART0_TX_IRQHandler` 중복 정의로 링크 실패(S3b). 템플릿은 매크로라 grep 을 피해간다.
- **무효한 방어코드를 넣고 안심하는 게 제일 위험하다** — 헤더 사전검사 1차 시도가
  재동기 경로를 안 타서 무효였는데 시뮬레이터가 잡아냈다(S4a+).
- **시험 도구도 자가검증이 필요하다** — `fg23_send.py` 자가검증이 스크립트 자신의
  LEN 계산 버그를 먼저 잡았다.
- **기능을 넣으면 기존 회귀 세트의 "미구현 기대"도 같이 손봐야 한다** — S4c 구현 후
  `--s4b` 의 `CH 3` 기대값이 낡아 오경보를 냈다.
- **로그가 설정과 어긋나면 안 된다** — `RING+IRQ`(실제는 폴링), `UART 115200`(실제 921600)
  두 번 발생. 하드코딩을 없애고 실제 값을 출력하도록 고쳤다.

---

## 6. 참조

| 문서 | 용도 |
|---|---|
| `../RF_STEP구현_SYNC.md` | **LTD_W10D 협업 창구** — 재개 시 먼저 읽을 것 |
| `../Network_Analyzer/FG23_UART_인터페이스.md` | UART 프레임·CSV·명령 스펙 (단일 기준) |
| `../Network_Analyzer/RF_동작_상세정의_WORKING.md` | RF 스펙 (§7 디코드 · §G 채널) |
| `../Network_Analyzer/1차작업_검증계획_5단계.md` | Phase 1~5 검증 계획 |
| `Software/NetAnalyzer_FG23_V01/step_config.h` | **스텝 계획 + 전 실측 이력 + 보류 결정** |
| `Software/_known_good/README.md` | 앵커 목록·롤백 절차 |
